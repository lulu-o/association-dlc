<!DOCTYPE html>
<html>
  <head>
    <title><%= meta_title %></title>
    <meta name="description" content="<%= meta_description %>">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Facebook Open Graph data -->
    <meta property="og:title" content="DLC" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="www.association-dlc.org" />
    <meta property="og:image" content="https://www.association-dlc.org/app/assets/images/og_image.jpg" />
    <meta property="og:description" content="D.L.C est une association qui lutte contre le gaspillage alimentaire, en récupérant les invendus des commerces pour les redistribuer à des associations caritatives et en libre accès." />
    <meta property="og:site_name" content="association-dlc" />

    <%= favicon_link_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body class="<%= "signed-in" if user_signed_in? %>">
    <%= render 'shared/navbar' if user_signed_in? %>
    <%= render 'shared/flashes' %>

    <%= yield %>
  </body>
</html>
