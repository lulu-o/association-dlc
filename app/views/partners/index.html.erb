<h1>TEST</h1>

<div class="d-flex">
     <%= render partial: 'form' %>
</div>

<div class="tabs d-flex">
  <div id="shops-list">Liste</div>
  <div id="shops-map">Carte</div>
</div>

<div class="shops-list">
  <% @partners.each do |partner| %>
    <div class="card-template-shop">
			<div class="card-shop">
				<div class="top">
					<div class="shop-name">
						<%= image_tag "logo-carrefour.jpeg", alt: "Carrefour", width: 30 %>
						<p class="shop-title"><%= partner.name %></p>
					</div>
					<div class="distance">
						<p class="distance-label"><span><i class="fas fa-map-marker-alt"></i></span>700 m</p>
					</div>
				</div>
				<div class="bottom">
					<p class="shop-address"><%= partner.full_address %></p>
					<p class="label-yellow">urgent</p>
				</div>
			</div>
		</div>
  <% end %>
</div>

<div class="shops-map">
  <div id="map" class="show"
    style="width: 100%; height: 500px;"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
</div>

